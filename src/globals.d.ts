// Build-time constants injected by Vite's `define` (see vite.config.ts).

// The year the bundle was built. Used for the footer copyright so the prerendered
// HTML and the hydrated client always render the identical value.
declare const __BUILD_YEAR__: number;
